# OPS日調アプリ UX・デザイン調査（2026-08-10）

依頼内容：実際にユーザーが使ったときに起きそうな問題・不便・欲しくなるもの・
見やすさの改善点を、忖度なしで洗い出す。**コードの変更はしない。**

## 0. 調査のやり方

- ローカルサーバー（`server/ops_nittyou.local.db`）を 8080 で起動し、
  `/i/<合言葉>` から **`apply.html` を実際に操作して申請を1件通した**
- `test/デザイン確認用.html` を静的配信し、`?role=staff` / `?role=admin` /
  `?screen=login` で各画面を実機同様に操作
- 画面幅 **390px（スマホ）と 1440px（PC）** の両方、**ダーク／ライト両テーマ**で確認
- 気になった箇所は `index.html` / `apply.html` / `style.css` / `server/server.js`
  の該当行を読んで裏を取った（行番号は本文中に記載）

未検証：本番Renderの環境変数（メール送信が実際に有効かどうか）、
Googleカレンダー連携の実動作、実機のタッチ操作（なぞり選択の指ざわり）。

---

## 1. 総評

**土台はかなりしっかりしている。** 権限まわり、保存の直列化、公開ページの
サーバー側再検証、キャッシュ、コメントの質——このへんは、外注で作らせたものと
しては相当に丁寧です。「作りが雑で作り直し」という話ではありません。

一方で **「機能を足し引きした跡が画面に残っている」** のが最大の弱点です。
インターン生のアカウントを廃止した影響が、文言・導線・機能の3か所で
取り残されていて、いま配布すると**ユーザーが確実に踏む死んだ導線が複数あります**。

そしてもうひとつ、**「面談を確定したあと、その結果を申請者に伝える手段が
アプリの中に一つも無い」**。これは設計上の穴で、機能追加ではなく
「このアプリは何をするものか」の線引きの問題です。ここは後述します。

---

## 2. 重大（配布前に判断すべき）

### 2-1. ログイン画面の「メールアドレスで会員登録する」は必ず失敗する

ログイン画面でいちばん目立つ緑のボタンです。押すと
メールアドレス・パスワード・ニックネーム・所属支部の入力フォームが開き、
一通り入力して「登録してはじめる」を押すと——**サーバーが 410 を返して終わり**です。

- `index.html:1217` … `switchLoginView('register-intern')`
- `index.html:1146-1163` … 廃止済みのインターン生登録フォームがそのまま残っている
- `server/server.js:1439-1443` … `register-intern` は常に 410

新しいスタッフが「アカウント無いから会員登録だな」と押す確率は高いです。
その下の目立たない「スタッフの方はこちら」が正解、という状態になっています。

### 2-2. 「パスワードを忘れた」への入口が、ログイン画面に無い

再設定の画面（`LOGIN_VIEW==='forgot'`）も、メール送信のAPIも、
トークンの発行・失効も**全部実装済み**です。ところが
`switchLoginView('forgot')` を呼んでいる箇所はプロジェクト全体で1つだけ、
しかもそれは **「再設定リンクが期限切れだったとき」の画面**（`index.html:1275`）。

つまり **パスワードを忘れた人は、その画面に永久にたどり着けません。**
作った機能が導線ごと抜け落ちている状態です。

- あわせて要確認：本番Renderで `MAIL_ENABLED` が真か（`server/mail.js`）。
  Renderの無料プランはSMTPの25/465/587を遮断するので、Brevo等の2525番へ
  切り替え済みでなければ、導線を足しても**メールは届きません**。
  ここが死んでいると「パスワードを忘れる＝管理者が手で直すまで詰み」です。

### 2-3. 面談を確定しても、申請者に伝える手段がアプリに無い

いまの申請フローで学生から取るのは **本名だけ**です（`apply.html`）。
メールアドレスも電話もLINE IDも聞いていません。

- 申請完了画面は「日程が決まりましたら、担当者からご連絡します」と出す
- しかし `/api/mail/send` は `receiver_id`（＝アカウントを持つユーザー）必須
  （`server/server.js:2088-2098`）。アカウントの無い申請者には**送れない**
- 確定した日時は、スタッフの画面とカレンダーには載るが、学生側には何も起きない

結果として、**確定連絡は100%アプリの外（LINE等）で人力**になります。
これは「不便」というより、日程調整アプリとしての中心的な価値が
半分抜けている状態です。設計として意図どおりなのかを確認したいところです。

対応の選択肢は3つあると思います。

1. **割り切る**：完了画面と申請フォームに「結果はLINEでお伝えします」と明記し、
   期待値を合わせる（コスト最小・今日できる）
2. **メールを任意で聞く**：申請フォームに「連絡先メール（任意）」を足し、
   確定時に自動送信。メール基盤が生きていることが前提
3. **申請者用の確認URL**：申請時に `/a/<ランダム>` を発行して完了画面に出し、
   「このURLを開けば状況が見られます」にする。**メール不要で実現でき、
   無料縛りとも相性が良い**ので、個人的にはこれを推します

### 2-4. 「担当一覧」は、もう永久に増えない画面

- `viewMyInterns()`（`index.html:3867`）は `DB.users` の中の
  `role==='intern'` を並べている
- いまの申請者は `intern_id=''` でユーザー行を作らない（`server/server.js:2795`）
- したがって **新規の申請者は担当一覧に一生出てこない**。出ているのは
  アカウント廃止前の古いインターン生だけ

さらに空のときの案内文が事実と違います。

> 「左メニューの『リンク作成』から登録リンクを渡すと、あなたが担当になります。」
> （`index.html:3874`）

実際のリンクは**支部で共通**で、担当は**学生が選択画面で選ぶ**ものです
（`index.html:1684-1689` の説明文のほうが正しい）。ホーム画面の
「登録リンクを渡すと、あなたが担当になります」（`index.html:1875`）も同じ誤りです。

同じ嘘が3か所（ホーム／サイドバーのtitle／担当一覧）にあります。

### 2-5. 依頼画面の説明文が、廃止済みの相手を案内している

> 「栃木のスタッフ・**インターン生**に、連絡や出欠確認を出せます。」
> （`index.html:4252`）

インターン生はログインできないので、依頼も出欠確認も届きません。

### 2-6. 祝日データが2026年ぶんの手打ちで、2027年に全部消える

`apply.html:97-102` と `index.html:1623-1628` に、
**同じ祝日テーブルが二重にハードコード**されています（コメントにも
「翌年以降は要更新」と書いてある）。

- 2027年1月1日を過ぎた瞬間、申請画面もカレンダーも**祝日が平日扱い**になる
- 二重管理なので、片方だけ直す事故が起きます
- しかも赤くなるだけで**祝日名は出ません**（`apply.html:359` の `<th>` に
  `title` すら無い）。ユーザーには「なぜか火曜が赤い」としか見えません

学生団体の運用で、毎年12月に誰かがこれを思い出せる可能性は低いと思います。

---

## 3. インターン生（apply.html）の使い勝手

実際に申請を1件通してみて気づいた点です。

### 3-1. スマホで、1週間のうち3日ぶんが画面の外にある

390px幅だと表に収まるのは **月〜木の4日だけ**。金・土・日は横スクロールが要ります。
しかも「もっと右にある」という手がかり（影・矢印・スクロールバー）が**何も出ません**。

実害も出ました。私が選んだ第3希望（8/14 金 19:00）は、
**選択後もセルが画面の外**で、緑の帯が右端に1mm見えるだけでした。
「ちゃんと選べているか」が確認できません。

### 3-2. 縦は28行 × 全部○の壁

受付時間の既定が全曜日9:00〜23:00なので、初期状態は
**196個の同じ○が並ぶだけの表**です。どこから手を付ければいいのか手がかりがゼロ。

- 午前／午後／夜の区切り線も、時間帯の背景色の差も無い
- スマホでは表の高さだけで約1,000px
- 「終日OK」は日付の下に7個並ぶが、**行き先が横スクロールの外にある日もある**

### 3-3. 「10:00〜10:30」が何を意味するか、まず伝わらない

10:00 と 10:30 の2枠を選ぶと、希望一覧には **「10:00〜10:30」** と出ます。
実際には「10:00開始と10:30開始のどちらでもOK（＝11:00まで空いている）」の意味です
（終わり＝最後の枠の**開始**時刻、という規則）。

学生は素直に「10時から10時半までの30分だけ空いている」と読みます。
**選べば選ぶほど、意図と表示がずれていく**規則になっています。
「10:00〜11:00の間で30分」のような書き方のほうが誤解が無いはずです。

### 3-4. 送信前の確認が無く、送信後に控えも残らない

- 「この3件で申請する」を押すと**即送信**。確認ステップ無し
- 完了画面に出るのは担当スタッフ名だけ。**自分が何を送ったかは表示されない**
- 取り消し・修正の導線が無い（間違えたら人に言うしかない）
- 同じURLをもう一度開けば**何度でも申請できる**（重複防止なし）

### 3-5. 希望の並べ替えがドラッグのみ

「ドラッグで並べ替えられます」と書いてありますが、スマホの縦スクロールと
ドラッグは競合しやすく、上下ボタン（↑↓）のほうが確実です。

### 3-6. キーボード・スクリーンリーダーでは操作できない

枠のセルは `<td onpointerdown="...">` で、`tabindex` も `role` も
`aria-pressed` もありません（`apply.html` の生成部）。
つまり **マウス／指がある人しか申請できません**。
公開ページで、相手が不特定多数の学生であることを考えると、これは弱点です。

---

## 4. スタッフ（index.html）の使い勝手

### 4-1. 面談一覧に「いつを希望しているか」が出ない

一覧の行は「名前・支部・申請○時間前・状態」だけ。
**第1希望の日時が出ないので、1件ずつ開かないと予定を組めません。**
申請が20件たまると、20回タップして20回閉じることになります。

並べ替えもできません（申請が新しい順で固定）。「希望日が近い順」が欲しくなる場面です。

### 4-2. いちばんやりたい操作が、いちばんボタンらしくない

面談の詳細シートで、目立つボタンは **「別日時で確定」と「不成立にする」** の2つ。
本命の「第1希望のとおり確定する」は、**リスト行に見える要素**を押して
展開してから「これで確定」を押します。

- 主要動作＝地味、例外動作＝目立つ、という逆転
- しかも「不成立にする」（赤・破壊的）が「別日時で確定」と同じ大きさ・同じ並び

### 4-3. 面談の長さが30分で固定（変えられない）

`server/slots.js:9` の `SLOT_MINUTES = 30` と、
確定時の `start + 30分`（`server/server.js:1116`, `3260`）がハードコードです。

- 1時間の面談を入れたい場合、表現する方法が無い
- 隣の30分枠は空いたままなので、**別の学生に埋められて二重予約**になり得ます

### 4-4. 「面談可能時間帯」と「面談できない時間を登録」

ホームの六角形は **「面談可能時間帯」**、開いた先の見出しは
**「面談できない時間を登録」**。同じ画面なのに意味が反転しています。

その画面には **「ホームへ戻る」が2つ縦に並びます**。
文言まで違います（「ホーム**へ**戻る」と「ホーム**に**戻る」）。

- `index.html:1740` … `render()` が全画面に付ける共通の戻るボタン
- `index.html:3220` … `viewAvailSet()` が自前でもう1つ付けている

### 4-5. 「日程調整」と「出欠確認」と「依頼」の区別がつかない

依頼画面の上に、**同じ色・同じ大きさのボタンが3つ**並びます。

| ボタン | 実体 |
|---|---|
| ＋ 日程調整 | 複数の候補日を出して ○△× を集める |
| ＋ 出欠確認 | 日付が1つ決まった予定への参加可否を集める |
| ＋ 依頼を出す | 日程が絡まない連絡・お願い |

さらに「日程調整」を開くと、シートの中に
**「送るもの：連絡・依頼／日程調整」というもう一段の切り替え**があります。
外で選んだのに中でまた選ぶ形で、「出欠確認」だけがその切り替えに入っていません。

用語も割れています。日程調整は「○参加できる／△たぶん／×無理」、
イベント出欠は「○参加／△未定／×不参加」。同じ3状態に2つの語彙があります。

なお「出欠確認」は**あて先が支部の全スタッフ固定**で、相手を選べません。

### 4-6. 送信した依頼だけ、置き場所が違う

「受けた依頼／完了済みの依頼」はページ上部のタブなのに、
**「送信した依頼」だけ画面右下に浮いているボタン**です。3つ目のタブが自然です。

---

## 5. 管理者の使い勝手

### 5-1. 全体管理者は、現場を何も見られない

`NAV` の定義（`index.html`）はこうなっています。

| 権限 | メニュー |
|---|---|
| staff | ホーム／面談一覧／担当一覧／依頼 |
| branch_admin | ＋ ユーザー |
| **admin** | **ホーム／支部／ユーザー のみ** |

全体管理者には**面談一覧も依頼も出ません**。
「全社でいま何件の面談が滞留しているか」を見る画面が無い状態です。
権限としては最上位なのに、できることがいちばん少ない。

### 5-2. ユーザー管理は支部数に対して破綻する

`viewUsers()`（`index.html:3705-3726`）は**全支部を1枚のページに縦に並べます**。
テストデータの37支部で、大半が「ユーザーなし」のカード。

- 検索欄なし、絞り込みなし、空支部の折りたたみなし
- 実測でページの高さ **約6,700px**。目的の人にたどり着くまで延々スクロール
- ドットジェイピーの支部数を考えると、ここは最初に音を上げる画面だと思います

### 5-3. 権限の選択肢に、使えない「インターン生」が残っている

ユーザー編集シートで役割に `intern` を選べます（`index.html:3733`）。
選んでもその人はログインできません。

### 5-4. 手元の `管理者ログイン情報.txt` の記述が古い

> 「登録後は管理者の**承認**が必要」

承認フローは廃止済みです（`server/server.js:1623`「登録と同時に即アクティブ」）。
また同ファイルの `?staff=...` 形式の招待URLも、
`register-staff` が廃止されている（`server/server.js:1448`）ため、
いまの正しい入口は `/staff` です。**このファイルは他人に渡す想定のはずなので、
内容が古いのは実害があります。**

---

## 6. デザイン・見やすさ

先に良い点を。**本文のコントラストは十分**です（`--sub` は白72%＝黒地に対し
約9.6:1）。「文字が薄くて読めない」系の問題はありませんでした。
出欠確認の集計画面（日程1行に○△×の数、最有力バッジ）は素直で分かりやすく、
ここは手を入れる必要が無いと思います。

### 6-1. 既定のダークテーマのほうが、情報が少ない

六角形タイルの色分け（面談＝緑／依頼＝オレンジ／時間帯＝青／メール＝ピンク）は
**`:root[data-theme="light"]` の中でしか定義されていません**
（`style.css:111`, `125-132`）。

- ダーク（既定）では4つとも同じグレー。地色 `rgb(38,38,38)` は
  背景 `#000` に対して **約1.4:1** で、輪郭がほぼ見えません
- 同じ理由で、画面ごとの差し色（`TAB_ACC`）もダークでは効きません

**既定をライトにするだけで見やすさが一段上がります。**
少なくとも、ダークにも色分けを用意する価値はあります。

### 6-2. テーマの切り替えが深すぎる

プロフィール →「ほかの設定」の中。既定がダークなので、
「なんか黒くて見づらい」と思った人の大半はここにたどり着けません。

### 6-3. 六角形タイルは、スマホで無理をしている

375px幅で**4つを横1列**に詰めるため、1つ約85px。

- 「面談一覧・確定」「面談可能時間帯」が2行に折り返す
- 説明文は **9.5px**（`style.css:1416` の `.hxd`）、見出しは12px
- タップ範囲も六角形なので、四隅は反応しません

**2×2 に落とすか、スマホでは普通のリストにする**のが素直だと思います。
六角形はロゴのモチーフとして玄関（ログイン画面）だけに残す、という手もあります。

### 6-4. スマホのカレンダーで、予定名が読めない

チップが「19:00 …」で切れます。件名が1文字も見えないので、
結局タップしないと分かりません。**時刻を省いて件名を出す**ほうが役に立ちます。

### 6-5. ログイン画面の右カラムが空っぽ

「外部IDでログイン」の枠は縦に長く、Googleボタンが**上下200pxの余白の中で
ひとりぼっち**になっています。左カラムは上詰めなので、視覚的な釣り合いが崩れています。

「次回から自動ログイン」のチェックが**左右に1つずつ、計2個**あるのも
（どちらを押せばいいのか）迷いどころです。

### 6-6. PC画面が縦長のフォームだらけ

1440px幅でも、シート（依頼作成・出欠確認など）は中央の狭い1カラムに
縦積みされ、シート内で独立スクロールします。左右に900px以上の余白がある一方で、
中身は「件名→本文→締切→候補日」と延々下に伸びます。
**PCでは2カラムにする**だけで、スクロール量がかなり減ります。

### 6-7. イベントカードが1件あたり350px

○△×のボタンがPCで巨大（各300px幅）です。月に10件予定があると
ホームが3,500px伸びます。回答済みのものは畳む、ボタンを小さくする、
などの余地があります。

なお回答が0件のとき、投票バーの幅が `0/0*100` ＝ `NaN%` になります
（`index.html:2609-2611`）。表示は壊れませんが、意図した状態ではないはずです。

### 6-8. アイコンが無い

`index.html` / `apply.html` / `attendance.html` のいずれにも
`favicon` / `apple-touch-icon` / `manifest` / `meta description` がありません。

- 全アクセスで `/favicon.ico` が404（実際に確認）
- タブのアイコンが白紙。ブックマークしても見分けがつかない
- **スマホの「ホーム画面に追加」をしても、真っ白なアイコンが並ぶ**

OPSのロゴPNGは手元にあるので、ここは投資対効果がいちばん高い部類です。

---

## 7. 運用・セキュリティ・スケール

正直に書きます。致命的な穴は見つかりませんでしたが、
**「500人に配る前に決めておくべきこと」**がいくつかあります。

### 7-1. 回数制限がどこにも無い

`server/server.js` 全体で、レート制限・試行回数制限の類は**1つもありません**。

- `POST /api/apply/:token` … 合言葉URLを知っていれば、
  でたらめな名前で**何百件でも申請を投げ込める**。1件ごとに通知も飛ぶ
- `POST /api/auth/login` … パスワードの**総当たりが無制限**。
  ロックも遅延もありません
- `PUT /api/attendance/:token/response` … 公開の出欠URLに
  **架空の回答者を無限に足せる**（しかも消す手段が無い）

合言葉URLは支部のLINEグループに貼られる前提なので、**漏れます**。
漏れたときに「作り直す」ボタンがあるのは良い設計ですが、
漏れる前提なら回数制限も欲しいところです。

### 7-2. ログインエラーがアカウントの有無を教えてしまう

Google登録のアドレスだと専用の文言が返るので（`server/server.js:1461-1463`）、
「そのメールアドレスが登録済みか」を外から判定できます。
親切さとのトレードオフですが、総当たり無制限と組み合わさると効いてきます。

### 7-3. お知らせがアプリの外に出ない

リアルタイム通知はSSEで実装されていますが、**タブを開いている間だけ**です。

- ブラウザのプッシュ通知は無し
- メールも（面談の確定・不成立以外は）飛ばない
- Render無料プランは15分で寝る → 久しぶりに開くと20〜60秒待たされる

「申請が来たことに誰も気づかない」が起きやすい構造です。
無料でできる対策としては、**Web Push**（無料・サーバー実装は必要）か、
割り切って**「1日1回開いてね」を運用ルールにする**かだと思います。

### 7-4. Googleカレンダー連携は100人で頭打ち

これは既知だと思いますが、審査未通過だと**プロジェクトの全期間で
100ユーザーまで**（変更・リセット不可）。配布前に審査を通す必要があります。

---

## 8. 「あったら欲しい」と言われそうなもの

現場から出てきそうな順に並べます。

1. **申請者への結果連絡**（§2-3）— 最優先
2. **面談一覧に第1希望の日時を出す／希望日順に並べる**
3. **面談の長さを選べる**（30分／60分）
4. **申請の取り消し・修正**（学生側）
5. **ユーザー管理の検索**
6. **リマインド**（前日に「明日は面談です」）
7. **月ごとの件数集計**（支部長が実績を報告するのに要りそう）
8. **面談メモ**（何を話したかをスタッフが残す欄。いまはどこにも書けない）
9. **同姓同名の見分け**（本名だけなので、同じ名前の申請が2件来ると詰む）
10. **CSV出力**（引き継ぎ・年度末に必ず言われるやつ）

---

## 9. 優先度つきの提案

| # | やること | 効果 | 手間 |
|---|---|---|---|
| 1 | 「会員登録」ボタンをスタッフ登録へ向ける（§2-1） | 大 | 小 |
| 2 | ログイン画面に「パスワードをお忘れの方」を出す（§2-2） | 大 | 小 |
| 3 | メール送信が本番で生きているか確認（§2-2） | 大 | 小 |
| 4 | 誤った文言4か所を直す（§2-4, §2-5） | 中 | 小 |
| 5 | 「ホームへ戻る」の重複を消す（§4-4） | 小 | 小 |
| 6 | favicon / アイコン / manifest を入れる（§6-8） | 中 | 小 |
| 7 | 既定テーマをライトにする（§6-1） | 中 | 小 |
| 8 | 面談一覧に第1希望を出す（§4-1） | 大 | 中 |
| 9 | 確定ボタンの主従を入れ替える（§4-2） | 中 | 中 |
| 10 | スマホの六角形を2×2に（§6-3） | 中 | 中 |
| 11 | 申請画面：横スクロールの手がかり／日別表示（§3-1） | 大 | 中 |
| 12 | 申請の確認ステップと完了画面の控え（§3-4） | 中 | 中 |
| 13 | 祝日を計算で出す（or 年またぎの警告）（§2-6） | 中 | 中 |
| 14 | 申請・ログインに回数制限（§7-1） | 中 | 中 |
| 15 | ユーザー管理に検索（§5-2） | 中 | 中 |
| 16 | 申請者向けの状況確認URL（§2-3） | 大 | 大 |
| 17 | 担当一覧を作り直すか撤去するか決める（§2-4） | 中 | 大 |
| 18 | 面談の長さを可変に（§4-3） | 中 | 大 |

1〜7は**半日で全部終わる範囲**で、しかも一番目立つ穴が塞がります。
ここだけ先にやる価値はあると思います。

---

## 10. この調査で私が触ったもの

- ローカルDB `server/ops_nittyou.local.db` を2点変更しました
  - 支部名 `b_test` を文字化けした値から **「東京支部」** に書き換え（表示確認のため）
  - **申請テストで面談を1件作成**（申請者名「調査 太郎」、担当＝テストスタッフ）
  - どちらもローカルのみ。**本番Turso・本番URLには一切アクセスしていません**
- `test/デザイン確認用.html` を `node tools/make-test-page.mjs` で作り直しました
  （`index.html` の最新版を反映するため。生成物なのでgit管理外）
- 確認用のスクリーンショットと一時サーバーは削除・停止済み
- なお `C:\Users\DoroL\.playwright-mcp\` に過去セッションぶんのログが
  溜まっています（今回の調査より前のものを含む）。整理するかは未判断

**どのファイルのコードも変更していません。**
